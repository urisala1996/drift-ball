{
  "name": "drift-ball",
  "version": "0.1.0",
  "description": "DRIFT·BALL — a low-poly, top-down arcade car football game. Pure-physics, car-sized ball, 1v1/2v2 vs AI.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
